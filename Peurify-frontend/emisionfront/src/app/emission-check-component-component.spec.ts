import { EmissionCheckComponentComponent } from './emission-check-component-component';

describe('EmissionCheckComponentComponent', () => {
  it('should create an instance', () => {
    expect(new EmissionCheckComponentComponent()).toBeTruthy();
  });
});
